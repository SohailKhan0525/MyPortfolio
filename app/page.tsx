"use client";

import { useEffect, useMemo, useRef, useState, type FormEvent, type KeyboardEvent, type ReactNode } from "react";
import Link from "next/link";
import { AnimatePresence, motion } from "motion/react";
import { ArrowDown, ArrowUpRight, Briefcase, CaretDown, Check, Code, GithubLogo, LinkedinLogo, List, Moon, PaperPlaneTilt, Sparkle, Sun, X } from "@phosphor-icons/react";

const projects = [
  { number: "01", title: "House Price Prediction", type: "Regression", description: "A machine learning project focused on predicting house prices with Python and scikit learn.", stack: ["Python", "pandas", "NumPy", "scikit learn"], href: "https://housepriceprediction-rqo8ykwgldxbpemwl6kvok.streamlit.app/", repo: "https://github.com/SohailKhan0525/HousePricePrediction" },
  { number: "02", title: "Global Earthquake Prediction", type: "Classification", description: "An applied classification project exploring earthquake related prediction with a machine learning workflow.", stack: ["Python", "pandas", "NumPy", "scikit learn"], href: "https://github.com/SohailKhan0525/Global-Earthquake-Prediction", repo: "https://github.com/SohailKhan0525/Global-Earthquake-Prediction" },
  { number: "03", title: "Heart Disease Prediction", type: "Logistic regression", description: "A Streamlit machine learning app that turns a classification model into an interactive prediction experience.", stack: ["Python", "scikit learn", "Streamlit"], href: "https://heartdiseaseml-4zrcurmudxpfxbwygcuyytm.streamlit.app/", repo: "https://github.com/SohailKhan0525/HeartDiseaseML" },
  { number: "04", title: "Student Management", type: "Python app", description: "A student management application built as a practical Python project with a hosted Streamlit interface.", stack: ["Python", "Streamlit"], href: "https://studentmanagementproject-ulj54ytmcyj55upzakbzhn.streamlit.app/", repo: "https://github.com/SohailKhan0525/StudentManagementProject" },
  { number: "05", title: "Student Pass / Fail Predictor", type: "Classification", description: "A classification project that packages a student outcome model into a small interactive application.", stack: ["Python", "scikit learn", "Streamlit"], href: "https://studentfailpasspredictor-hjymwbpsp4bksec9ycb2xz.streamlit.app/", repo: "https://github.com/SohailKhan0525/StudentFailPassPredictor" },
  { number: "06", title: "Bank Management", type: "Python app", description: "A practical bank management project focused on core application logic and Python fundamentals.", stack: ["Python", "Streamlit"], href: "https://bankmanagementproject-brt9pt282uavy8ddzzy7ql.streamlit.app/", repo: "https://github.com/SohailKhan0525/BankManagementProject" },
  { number: "07", title: "File Handling", type: "Python CLI", description: "A focused Python project for working with files and building confidence with everyday application code.", stack: ["Python", "OOP", "pathlib", "JSON"], href: "https://github.com/SohailKhan0525/File_Handling_Project", repo: "https://github.com/SohailKhan0525/File_Handling_Project" },
];

const skills = [["Python", "Primary language"], ["scikit learn", "ML workflows"], ["pandas", "Data analysis"], ["NumPy", "Numerical computing"], ["Streamlit", "Interactive apps"], ["Git / GitHub", "Version control"], ["HTML / CSS / JS", "Web foundations"], ["Next.js", "Portfolio stack"]];
type Theme = "system" | "light" | "dark";

function playTone(kind: "tap" | "success" | "hover") {
  if (typeof window === "undefined") return;
  try {
    const AudioContextCtor = window.AudioContext || (window as typeof window & { webkitAudioContext?: typeof AudioContext }).webkitAudioContext;
    if (!AudioContextCtor) return;
    const ctx = new AudioContextCtor(); const oscillator = ctx.createOscillator(); const gain = ctx.createGain(); const now = ctx.currentTime;
    const frequencies = kind === "success" ? [392, 523.25, 659.25] : kind === "hover" ? [520] : [280, 360];
    oscillator.frequency.setValueAtTime(frequencies[0], now); frequencies.slice(1).forEach((f, i) => oscillator.frequency.setValueAtTime(f, now + (i + 1) * 0.045)); oscillator.type = "sine";
    gain.gain.setValueAtTime(0.0001, now); gain.gain.exponentialRampToValueAtTime(kind === "hover" ? 0.012 : 0.035, now + 0.012); gain.gain.exponentialRampToValueAtTime(0.0001, now + (kind === "success" ? 0.24 : 0.12));
    oscillator.connect(gain).connect(ctx.destination); oscillator.start(now); oscillator.stop(now + (kind === "success" ? 0.25 : 0.13)); oscillator.addEventListener("ended", () => void ctx.close());
  } catch {}
}

function useSoundEnabled() {
  const [enabled, setEnabled] = useState(true);
  useEffect(() => setEnabled(localStorage.getItem("portfolio-sound") !== "off"), []);
  const toggle = () => setEnabled(current => { const next = !current; localStorage.setItem("portfolio-sound", next ? "on" : "off"); if (next) playTone("success"); return next; });
  return { enabled, toggle };
}

function useTheme() {
  const [theme, setTheme] = useState<Theme>("system"); const [resolved, setResolved] = useState<"light" | "dark">("dark");
  useEffect(() => { const stored = (localStorage.getItem("portfolio-theme") as Theme | null) ?? "system"; const media = window.matchMedia("(prefers-color-scheme: light)"); const apply = (t: Theme) => { const r = t === "system" ? (media.matches ? "light" : "dark") : t; setResolved(r); document.documentElement.dataset.theme = r; }; setTheme(stored); apply(stored); const listener = () => apply(stored); media.addEventListener("change", listener); return () => media.removeEventListener("change", listener); }, []);
  const change = (next: Theme) => { localStorage.setItem("portfolio-theme", next); setTheme(next); const r = next === "system" ? (window.matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark") : next; setResolved(r); document.documentElement.dataset.theme = r; };
  return { theme, resolved, change };
}

function Reveal({ children, className = "", delay = 0 }: { children: ReactNode; className?: string; delay?: number }) {
  return <motion.div className={className} initial={{ opacity: 0, y: 36, filter: "blur(8px)" }} whileInView={{ opacity: 1, y: 0, filter: "blur(0px)" }} viewport={{ once: true, amount: 0.14 }} transition={{ duration: 0.8, delay, ease: [0.32, 0.72, 0, 1] }}>{children}</motion.div>;
}

function ThemeMenu({ theme, resolved, change }: { theme: Theme; resolved: "light" | "dark"; change: (theme: Theme) => void }) {
  const [open, setOpen] = useState(false);
  return <div className="theme-control"><button className="icon-button theme-button" aria-expanded={open} aria-haspopup="menu" onClick={() => { setOpen(v => !v); playTone("tap"); }}>{resolved === "dark" ? <Moon size={16} weight="bold" /> : <Sun size={16} weight="bold" />}<span>{theme}</span><CaretDown size={13} weight="bold" className={open ? "rotate" : ""} /></button><AnimatePresence>{open && <motion.div className="theme-menu" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 8 }}>{(["system", "light", "dark"] as Theme[]).map(item => <button key={item} className={`theme-option ${theme === item ? "selected" : ""}`} onClick={() => { change(item); setOpen(false); playTone("success"); }}><span>{item}</span>{theme === item && <Check size={14} weight="bold" />}</button>)}</motion.div>}</AnimatePresence></div>;
}

function HeroTerminal({ soundEnabled }: { soundEnabled: boolean }) {
  const commands = ["help", "about", "projects", "skills", "contact", "clear"];
  const [input, setInput] = useState(""); const [history, setHistory] = useState<string[]>([]); const [historyIndex, setHistoryIndex] = useState(-1); const [booted, setBooted] = useState(false); const inputRef = useRef<HTMLInputElement>(null);
  useEffect(() => { const timer = window.setTimeout(() => setBooted(true), 520); return () => window.clearTimeout(timer); }, []);
  const outputFor = (command: string) => {
    if (command === "help") return ["help      show available commands", "about     whoami + current focus", "projects  list selected work", "skills    show current stack", "contact   jump to contact", "clear     clear terminal"];
    if (command === "about" || command === "whoami") return ["Mohd Zaheer Uddin", "CSIT undergraduate · ML & Data Science", "building practical Python + ML projects."];
    if (command === "projects" || command === "ls") return projects.map(p => `${p.number}  ${p.title}`);
    if (command === "skills") return ["Python · scikit learn · pandas · NumPy", "Streamlit · Git/GitHub · HTML/CSS/JS · Next.js"];
    if (command === "contact") return ["Opening contact section…"];
    return [`command not found: ${command}`, `type \"help\" to see available commands`];
  };
  const submit = (event: FormEvent) => { event.preventDefault(); const command = input.trim().toLowerCase(); if (!command) return; if (command === "clear") { setHistory([]); setInput(""); setHistoryIndex(-1); if (soundEnabled) playTone("tap"); return; } setHistory(current => [...current, `$ ${input}`, ...outputFor(command)]); setInput(""); setHistoryIndex(-1); if (command === "contact") window.setTimeout(() => document.querySelector("#contact")?.scrollIntoView({ behavior: "smooth" }), 100); if (soundEnabled) playTone("success"); };
  const onKeyDown = (event: KeyboardEvent<HTMLInputElement>) => { const items = history.filter(line => line.startsWith("$ ")).map(line => line.slice(2)); if (event.key === "Tab") { event.preventDefault(); const match = commands.find(item => item.startsWith(input.toLowerCase())); if (match) setInput(match); } if (event.key === "ArrowUp") { event.preventDefault(); if (!items.length) return; const next = Math.min(historyIndex + 1, items.length - 1); setHistoryIndex(next); setInput(items[items.length - 1 - next] ?? ""); } if (event.key === "ArrowDown") { event.preventDefault(); if (historyIndex <= 0) { setHistoryIndex(-1); setInput(""); return; } const next = historyIndex - 1; setHistoryIndex(next); setInput(items[items.length - 1 - next] ?? ""); } };
  return <div className="hero-terminal-real" onClick={() => inputRef.current?.focus()}>
    <style>{`.hero-terminal-real{position:relative;overflow:hidden;border:1px solid var(--line-strong);border-radius:24px;background:radial-gradient(circle at 78% 0%,rgba(255,255,255,.11),transparent 38%),#080808;box-shadow:0 28px 90px var(--shadow),inset 0 1px 0 rgba(255,255,255,.08);transform:perspective(1200px) rotateY(-2deg) rotateX(1deg);transition:transform .7s cubic-bezier(.32,.72,0,1),box-shadow .7s cubic-bezier(.32,.72,0,1)}.hero-terminal-real:hover{transform:perspective(1200px) rotateY(0) rotateX(0) translateY(-6px);box-shadow:0 36px 100px var(--shadow)}.hero-terminal-real:before{content:"";position:absolute;inset:52px 0 0;pointer-events:none;background:repeating-linear-gradient(to bottom,transparent 0,transparent 3px,rgba(255,255,255,.018) 4px);mask-image:linear-gradient(to bottom,black,transparent 96%)}.terminal-top{height:52px;display:flex;align-items:center;justify-content:space-between;padding:0 16px;border-bottom:1px solid var(--line);background:rgba(255,255,255,.045);font:500 11px/1 "Geist Mono",monospace;color:var(--muted)}.terminal-top-left{display:flex;align-items:center;gap:12px}.terminal-dots{display:flex;gap:6px}.terminal-dots i{width:8px;height:8px;border-radius:50%;background:var(--muted);opacity:.55}.terminal-tab{padding:8px 12px;border:1px solid var(--line);border-radius:9px;background:rgba(255,255,255,.035);color:var(--text)}.terminal-status{display:flex;align-items:center;gap:7px}.terminal-status i{width:6px;height:6px;border-radius:50%;background:var(--text);box-shadow:0 0 12px var(--text)}.terminal-screen{position:relative;min-height:410px;padding:24px;font:500 13px/21px "Geist Mono",monospace;color:#bcbcbc}.terminal-boot{margin-bottom:18px;color:#777}.terminal-boot b{color:#fff}.terminal-history{display:grid;gap:2px;min-height:176px}.terminal-line{white-space:pre-wrap}.terminal-command{color:#fff}.terminal-command:before{content:"› ";color:#8f8f8f}.terminal-output-line{color:#8e8e8e}.terminal-form{display:flex;align-items:center;gap:9px;margin-top:18px;padding-top:14px;border-top:1px solid var(--line)}.terminal-prompt{color:#fff}.terminal-input{min-width:0;flex:1;border:0;outline:0;background:transparent;color:#fff;font:inherit;caret-color:#fff}.terminal-input::placeholder{color:#5f5f5f}.terminal-cursor{width:7px;height:16px;background:#fff;opacity:.85;animation:terminalBlink 1s steps(1) infinite}@keyframes terminalBlink{50%{opacity:0}}.terminal-help{display:flex;justify-content:space-between;gap:12px;margin-top:12px;color:#555;font-size:10px}.terminal-quick{display:flex;gap:6px;flex-wrap:wrap}.terminal-chip{padding:5px 8px;border:1px solid var(--line);border-radius:7px;background:transparent;color:#777;font:500 10px/1 "Geist Mono",monospace;transition:.25s}.terminal-chip:hover{color:#fff;border-color:var(--line-strong);background:rgba(255,255,255,.06)}.terminal-bottom{display:flex;justify-content:space-between;padding:11px 16px;border-top:1px solid var(--line);background:rgba(255,255,255,.025);color:#666;font:500 10px/1 "Geist Mono",monospace}.terminal-bottom span:last-child{color:#999}@media(max-width:800px){.hero-terminal-real,.hero-terminal-real:hover{transform:none}.terminal-screen{min-height:390px;padding:20px 18px}.terminal-tab{display:none}.terminal-help{display:none}}@media(prefers-reduced-motion:reduce){.hero-terminal-real,.hero-terminal-real:hover{transform:none}.terminal-cursor{animation:none}}`}</style>
    <div className="terminal-top"><div className="terminal-top-left"><span className="terminal-dots"><i/><i/><i/></span><span className="terminal-tab">portfolio.py</span><span>zsh — portfolio</span></div><span className="terminal-status"><i/> local · ready</span></div>
    <div className="terminal-screen"><div className="terminal-boot"><b>portfolio@zaheer</b>:~$ ./start.sh<br/><span>{booted ? "profile loaded · projects loaded · terminal ready." : "initializing portfolio runtime…"}</span></div><div className="terminal-history" aria-live="polite">{history.slice(-9).map((line, index) => <div key={`${line}-${index}`} className={line.startsWith("$ ") ? "terminal-line terminal-command" : "terminal-line terminal-output-line"}>{line}</div>)}</div><form className="terminal-form" onSubmit={submit}><span className="terminal-prompt">$</span><input ref={inputRef} className="terminal-input" value={input} onChange={event => setInput(event.target.value)} onKeyDown={onKeyDown} placeholder={booted ? "type a command…" : "initializing…"} disabled={!booted} aria-label="Terminal command input" autoComplete="off" spellCheck={false}/><span className="terminal-cursor" aria-hidden="true"/></form><div className="terminal-help"><span>↑↓ history · Tab autocomplete · Enter run</span><div className="terminal-quick">{["help", "projects", "skills", "about"].map(command => <button key={command} type="button" className="terminal-chip" onClick={() => { setInput(command); inputRef.current?.focus(); }}>{command}</button>)}</div></div></div><div className="terminal-bottom"><span>PORTFOLIO SHELL v1.0</span><span>next.js · client runtime</span></div>
  </div>;
}

export default function Home() {
  const [mobileOpen, setMobileOpen] = useState(false); const [activeProject, setActiveProject] = useState(0); const [copied, setCopied] = useState(false);
  const { theme, resolved, change } = useTheme(); const sound = useSoundEnabled();
  const navItems = useMemo(() => [["Work", "#work"], ["About", "#about"], ["Journey", "#journey"], ["Skills", "#skills"], ["Resume", "/resume"], ["Contact", "#contact"]], []);
  const email = "sohailkhannnn.0525@gmail.com";
  const copyEmail = async () => { try { await navigator.clipboard.writeText(email); setCopied(true); if (sound.enabled) playTone("success"); window.setTimeout(() => setCopied(false), 1600); } catch { window.location.href = `mailto:${email}?subject=Portfolio%20Contact`; } };
  const project = projects[activeProject];
  return <div className="site-shell"><div className="noise" aria-hidden="true"/><div className="grid-lines" aria-hidden="true"/>
    <header className="site-header"><Link href="#top" className="brand" onClick={() => sound.enabled && playTone("tap")}><span className="brand-mark">Q</span><span>Mohd Zaheer Uddin</span></Link><nav className={`desktop-nav ${mobileOpen ? "mobile-visible" : ""}`} aria-label="Primary navigation">{navItems.map(([label, href], index) => <Link key={label} href={href} className="nav-link" onClick={() => { setMobileOpen(false); if (sound.enabled) playTone(index === 0 ? "success" : "tap"); }}>{label}</Link>)}</nav><div className="header-actions"><ThemeMenu theme={theme} resolved={resolved} change={change}/><button className="icon-button sound-button" onClick={() => sound.toggle()} aria-label={sound.enabled ? "Mute interface sounds" : "Enable interface sounds"} aria-pressed={sound.enabled}><span className={`sound-bars ${sound.enabled ? "on" : ""}`}><i/><i/><i/></span></button><button className="mobile-menu-button" onClick={() => setMobileOpen(v => !v)} aria-expanded={mobileOpen} aria-label="Toggle navigation"><AnimatePresence mode="wait" initial={false}>{mobileOpen ? <motion.span key="x"><X size={20}/></motion.span> : <motion.span key="list"><List size={20}/></motion.span>}</AnimatePresence></button></div></header>
    <main id="top"><section className="hero section-pad"><div className="hero-copy"><Reveal><p className="eyebrow"><span className="status-dot"/> CSIT undergraduate · ML &amp; Data Science</p></Reveal><Reveal delay={0.08}><h1>I build practical machine learning projects and data driven tools.</h1></Reveal><Reveal delay={0.16}><p className="hero-description">I am Mohd Zaheer Uddin. I work mainly with Python, scikit learn, pandas, NumPy, and Streamlit while building a stronger foundation in machine learning.</p></Reveal><Reveal delay={0.24}><div className="hero-actions"><a className="button button-primary" href="#work" onClick={() => sound.enabled && playTone("success")}>See the work <ArrowDown size={17} weight="bold"/></a><button className="text-button" onClick={copyEmail}>{copied ? "Email copied" : "Copy email"} <PaperPlaneTilt size={16} weight="bold"/></button></div></Reveal><Reveal delay={0.32}><div className="proof-row"><span>7 projects</span><span>Python + ML focus</span><span>Open to internships</span></div></Reveal></div><Reveal className="hero-art" delay={0.18}><HeroTerminal soundEnabled={sound.enabled}/><div className="hero-note">Try <b>help</b>, <b>projects</b>, <b>skills</b>, or <b>about</b> — the terminal is actually interactive.</div></Reveal></section>
      <Reveal className="tagline-wrap section-pad"><div className="tagline"><span className="eyebrow">The point of this portfolio</span><p>Show the work, the reasoning behind it, and what I am learning next.</p></div></Reveal>
      <section id="work" className="section-pad section-block"><Reveal className="section-heading"><div><p className="eyebrow">Selected work</p><h2>Projects with a purpose.</h2></div><p>Each project is small enough to explain and real enough to show how I work.</p></Reveal><div className="work-layout"><div className="project-list">{projects.map((item, index) => <motion.button key={item.number} className={`project-row ${activeProject === index ? "active" : ""}`} onMouseEnter={() => setActiveProject(index)} onFocus={() => setActiveProject(index)} onClick={() => { setActiveProject(index); if (sound.enabled) playTone("tap"); }} whileTap={{ scale: .985 }}><span className="project-number">{item.number}</span><span className="project-name"><strong>{item.title}</strong><small>{item.type}</small></span><ArrowUpRight className="project-arrow" size={17}/></motion.button>)}</div><AnimatePresence mode="wait"><motion.article key={project.number} className="project-detail surface" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -12 }}><div><div className="detail-top"><span className="eyebrow">{project.type}</span><span>{project.number}</span></div><h3>{project.title}</h3><p>{project.description}</p><div className="stack-row">{project.stack.map(item => <span key={item}>{item}</span>)}</div></div><div className="detail-actions"><a className="button button-primary" href={project.href} target="_blank" rel="noreferrer">Open project <ArrowUpRight size={16}/></a><a className="button button-secondary" href={project.repo} target="_blank" rel="noreferrer"><GithubLogo size={17}/> Source</a></div></motion.article></AnimatePresence></div></section>
      <section id="about" className="section-pad section-block"><Reveal className="section-heading"><div><p className="eyebrow">About</p><h2>Curious, practical, still learning.</h2></div><p>I like projects that turn concepts into something someone can actually click, test, or understand.</p></Reveal><div className="about-grid"><div className="about-copy"><p>I am a CSIT undergraduate focused on building a strong foundation in Python, machine learning, data analysis, and modern web development.</p><p>My portfolio is intentionally honest: practical projects, visible learning, and a steady move from small experiments toward better engineering.</p><div className="about-facts"><span><Briefcase size={17}/> Open to internships</span><span><Code size={17}/> Python + ML</span><span><Sparkle size={17}/> Learning by building</span></div></div><div className="surface github-card"><div className="github-copy"><p className="eyebrow">Open source trail</p><h2>Build in public.</h2><p>See the code, experiments, commits, and projects behind the portfolio.</p><a className="button button-secondary" href="https://github.com/SohailKhan0525" target="_blank" rel="noreferrer"><GithubLogo size={17}/> GitHub <ArrowUpRight size={15}/></a></div><div className="contribution-wrap"><img src="https://ghchart.rshah.org/9B9B9B/SohailKhan0525" alt="GitHub contribution chart for SohailKhan0525"/><div className="contribution-caption">github activity · learning in public</div></div></div></div></section>
      <section id="journey" className="section-pad section-block"><Reveal className="section-heading"><div><p className="eyebrow">Journey</p><h2>From fundamentals to systems.</h2></div><p>A simple progression: understand the basics, build projects, then make the projects better.</p></Reveal><div className="journey-grid">{[["01", "Python foundations", "Core syntax, OOP, files, data structures"], ["02", "ML experiments", "Regression, classification, evaluation"], ["03", "Interactive apps", "Turning models into usable Streamlit tools"], ["04", "Modern web", "Next.js, responsive UI, motion, deployment"]].map(([number, title, description]) => <Reveal key={number}><article className="journey-item"><span>{number}</span><h3>{title}</h3><p>{description}</p></article></Reveal>)}</div></section>
      <section id="skills" className="section-pad section-block"><Reveal className="section-heading"><div><p className="eyebrow">Skills</p><h2>The current toolkit.</h2></div><p>Tools I use today, plus the ones I am actively getting better with.</p></Reveal><div className="skills-grid">{skills.map(([title, description], index) => <motion.article key={title} className="skill-card surface" whileHover={{ y: -4 }}><span className="skill-index">0{index + 1}</span><strong>{title}</strong><span>{description}</span></motion.article>)}</div></section>
      <section id="contact" className="section-pad section-block"><Reveal><div className="surface contact-card"><div><p className="eyebrow">Contact</p><h2>Let’s build something useful.</h2><p>If you are looking for an intern, collaborator, or someone who enjoys learning by building, I would love to hear from you.</p></div><div className="contact-actions"><button className="button button-primary" onClick={copyEmail}>{copied ? "Email copied" : "Copy email"} <PaperPlaneTilt size={17}/></button><a className="button button-secondary" href="https://www.linkedin.com" target="_blank" rel="noreferrer"><LinkedinLogo size={17}/> LinkedIn</a><a className="button button-secondary" href="https://github.com/SohailKhan0525" target="_blank" rel="noreferrer"><GithubLogo size={17}/> GitHub</a></div></div></Reveal></section>
    </main><footer className="site-footer section-pad"><span>© 2026 Mohd Zaheer Uddin</span><span>Built with Next.js · Python mindset · shipped on Vercel</span></footer>
  </div>;
}
